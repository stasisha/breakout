export default class Ball {
  constructor(x, y, ballRadius, fillStyle) {
    this.type = 'arc';
    this.x = x;
    this.y = y;
    this.ballRadius = ballRadius;
    this.fillStyle = fillStyle;
  }
}
